 import 'package:flutter/material.dart';

class SongsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Songs Tab Content'),
    );
  }
}
